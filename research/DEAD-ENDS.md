# DEAD-ENDS — hipóteses refutadas neste projeto

Propósito: **impedir trabalho em círculos**. Antes de tentar consertar
qualquer coisa, confira se a abordagem já foi refutada nesta base. Retomar um
dead end exige evidência nova e mensurável (não "faz sentido").

Regras:

1. Toda entrada tem estado `REFUTADO` + a evidência que mediu.
2. Entradas novas só entram com comando/observação que refuta.
3. A alternativa válida (o que FUNCIONA) está em cada entrada — use ela.

| # | Hipótese refutada | Evidência | Alternativa válida |
|---|---|---|---|
| 1 | Timestamp do envelope como epoch (`timestamp`), `yyyy-MM-dd HH:mm:ss`, ou chaves `timestamp`/`utc_timestamp` | `Ubu.GameController:ParseServerTimestamp` faz parse estrito; bisseção no emulador (RELATORIO-STATUS 2026-08-16) | Chave `uts` sozinha, `yyyy-MM-ddTHH:mm:ss` UTC ([index.js `wire()`](../server/src/index.js)) |
| 2 | `event_type` é campo do DTO do schedule (discriminador de subclasse de evento) | Bisseção do boot: `Malformed response payload` → abort após 3 tentativas quando presente; o DTO real do cluster v29 não o lista. Teste de regressão em `server/test/smoke.mjs` | Nunca enviar `event_type`; omissão de ausentes ([events.js](../server/src/events.js)) |
| 3 | Campo numérico sem valor pode ir como `null` no wire | `Malformed response payload` + abort no boot (mesma bisseção acima); regra fixa do parse Newtonsoft do cliente | Omitir o campo inteiro |
| 4 | `armory/get` pode responder sem `upgrades` | `ArmoryController.Init` faz `foreach` em `upgrades` null → NRE logo após login | Sempre `upgrades: []` (array vazio) |
| 5 | O endpoint da API está como string ASCII no ZIP do APK / no `global-metadata.dat` (scan cru acha) | Scan de bytes do ZIP não vê nada; decodificação UnityPy mostra que o `baseUrl` vive em bundle Addressables comprimido em LZ4 | Patch direto no metadata (hostname) + `patch_unity_bundle.py` com `--sweep-all-bundles` |
| 6 | Crescer o hostname no `global-metadata.dat` além do comprimento original | Crescer exigiria realocar seções e deslocar todos os offsets — o app não boota. `scripts/check_patch_length.py` exit 4 | Hostname ≤ orçamento de bytes (31 no 1.13.1) com padding de userinfo; senão caminho bundle-aware |
| 7 | Alterar bundle em `assets/aa/**` sem tocar o catálogo | `CRC Mismatch ... Will not load AssetBundle` → `RemoteProviderException` na carga de cena | `zero_catalog_crc` sempre após reserializar bundle ([patch_unity_bundle.py](../scripts/patch_unity_bundle.py)) |
| 8 | "Atualizar" UnityPy/apktool/uber-apk-signer para resolver problemas de resserialização | Outra versão reserializa diferente e invalida os testes de regressão do patcher; toolchain pinada com SHA-256 | UnityPy 1.25.3 exata, Apktool 3.0.3, uber-apk-signer 1.3.0 |
| 9 | Trocar o token de sessão por JWT para consertar o travamento em 100% | O logcat registra `Session token is not a well formed JWT as expected` (warning), mas o boot COMPLETA com o token base64url atual — causa real era o contrato do `get-schedule`. RELATORIO-STATUS 2026-08-16 | Manter token opaco **para aquele sintoma**. ATUALIZAÇÃO 2026-08-20: o JWT foi adotado por outro motivo (`server/src/jwt.js`) e está validado em restart real no rig — `login-device` → user-data, **zero** ocorrências do warning. Contrato obrigatório: `aud`/`audience` como ARRAY (string crua derruba `UpdateSessionToken`). A refutação original continua de pé: JWT não era a cura do travamento em 100% |
| 10 | Rotas inventadas/transcritas à mão (`talents/get`, `chapters/stage-rewards`, `reward-tracks/get-state`, ...) | Comparação server × 116 rotas do metadata: 10 literais do servidor não existem no cliente (ver `server_only_routes` em `compatibility.json`) | Toda rota vem de `scripts/dump_il2cpp_metadata.py`; o registro acusa drift |
| 11 | `RESEARCH_MODE` respondendo `ok()` vazio é compatibilidade | Cliente aceita HTTP 200 vazio sem abortar, mas a funcionalidade não existe — é fallback de pesquisa, contado em `/revival/research` e bloqueado no gate de fluxos validados | Implementar o contrato real; `verify_everything.py` falha se fluxo validado usar fallback |
| 12 | Marcar endpoint como done porque respondeu HTTP 200 | HTTP 200 com payload vazio/errado ainda derruba o cliente (`Malformed response payload`) ou finge função | DoD completo só com os 8 gates do `compatibility.json` |
| 13 | `idle_reward.generation_period` pode ir ao wire como INTEIRO de segundos | Bisseção no rig 2026-08-20 (`work/audit-opus/rig/lab-bisect-*`): com inteiro, `Malformed response payload` em `Network response (14)`; sem o campo, `flow_validated`; com TimeSpan .NET `00:05:00`, falha de novo | Duração em TEXTO no formato do game-data: `0D00H05M00S` (`formatDuration()` em `server/src/rewards.js`). O valor em segundos fica interno, para a aritmética |
| 14 | `Number(x)` basta para normalizar campo numérico vindo do game-data | `Number('0D00H05M00S')` é **NaN**, e `JSON.stringify(NaN)` emite **null** — o wire saiu com `generation_period: null` e derrubou o boot (é o DEAD-ENDS #3 entrando por uma porta lateral) | `durationSeconds()` para duração e `stripNulls()` no `wire()` (`server/src/wire.js`), que omite null/undefined/NaN de toda resposta `/game/*` |
| 15 | Semear `gpg.config` com `hasCancelledLogin=true` suprime o popup do Google Play Games | Medido no AVD descartável 2026-08-20: com o arquivo semeado (round-trip byte a byte do real), o popup `USE GOOGLE PLAY GAMES?` **apareceu mesmo assim**; o que sumiu foi o erro do plugin no logcat | O popup some quando a `RevivalAuthActivity` é o único MAIN/LAUNCHER e entrega `credentials.json` válido antes de a Unity subir — 0 ocorrências do popup e nenhuma rota Google no fluxo. Re-verificado 2026-08-23 após o redesenho visual da Activity: login pela UI (conta `ui-register-test@example.com`, senha digitada pelo tester — não lida por ADB) até `get-daily-quests` (ids 1329-1345, sequência de boot padrão); 7 dumps UIAutomator ao longo do boot sem nenhum nó com `google`/`play games`/`gms.`/`signin`; logcat completo (4474 linhas) sem `GoogleApiActivity`/`SignInHubActivity`/`USE GOOGLE PLAY GAMES`/`Displayed com.google.android.gms` — as únicas linhas com "google" são ruído esperado (medição/dynamite do Firebase, WebView do sistema, e o campo `deviceOEM=Google` do payload do ironSource) |
| 16 | Repetir `tutorial/complete-sequence` para sequência já concluída é erro de cliente (`400/2000`) | request_log 287 do restart: o cliente REENVIA a conclusão e o 400 derrubou o parse com `Malformed response payload` | Idempotente: `{ resources: [] }` com code 1000, sem reconceder recompensa (`server/src/tutorial.js`) |
| 17 | `game/iap/*` pode responder `400/2000` em bloco porque IAP está desligado | request_log 323: o cliente chama `iap/get-purchase-history-info` no boot e o 400 quebrou o parse | Consulta read-only responde sucesso com `total_lifetime_purchase: 0`, omitindo `time_since_last_purchase`. Comprar continua desligado |
| 18 | O `device_id` para "Entrar" em instalação limpa precisa ser inventado ou vir de rota nova | `login-device` recebe `device_id` no REQUEST e não o devolve; mas `register` emite `device_id: body.device_id ou user.uuid` (index.js:595) e `login-device` emite `puuid: user.uuid` — e nenhum cliente envia `device_id` no register | Usar o `puuid` do response de `login-device`: é o MESMO valor, com outro nome de wire. Nenhuma rota `/revival/*` nova foi criada |
| 19 | Membro de coleção de Response pode ir no wire com o nome fallback snake_case do campo C# (`quests`, `tracks`) | Bisseção no rig 2026-08-23 (8 degraus, `work/final-shot/server-fs*.log`): array **vazia** sob o nome errado é tolerada (parse passa), mas com **conteúdo** o cliente derruba `Malformed response payload`. Os overrides `[JsonProperty("daily_quests")]` e `[JsonProperty("reward_tracks")]` estão no blob `attributeData` do metadata v29 e ganham do fallback | Nome de coleção de Response só depois de conferir o literal no attributeData (`dump_il2cpp_metadata.py --wire-names`). Medido: `daily_quests` (`GetDailyQuestsResponse.quests`) e `reward_tracks` (`GetAllResponse.tracks`) em [quests.js](../server/src/quests.js)/[reward-tracks.js](../server/src/reward-tracks.js) |
| 20 | O botão READY TO COLLECT do painel MISSIONS emite `game/quests/claim-daily-quest` | Rig 2026-08-23: 3 taps medidos nos botões dourados (coordenadas por pixel scan), janela harness `--no-launch` de 100 s com **0 requests**, 0 exceções no logcat, painel byte-idêntico antes/depois (`missions-claim*.png`). `DailyQuestController.UpdateQuestState` lança NRE em **toda** resposta de get-daily-quests (vazio OU cheio) — client-interno | Rota permanece `request_observed=false` com a negativa registrada; só reabrir com evidência nova de outra porta de entrada |
| 21 | Comprar/equipar cosmético emite `game/store/purchase`, `game/gear/apply-cosmetic` ou `game/slayers/apply-cosmetic` | Flows UI completos no rig 2026-08-23 (compra por moedas de uniforme e equip de uniforme + skin de arma, `work/final-shot/uniforms-fs*.png`, `weaponskins-fs*.png`): **zero** chamadas de rede e inventário do servidor inalterado entre cold boots — cosmético é client-authoritative no 1.13.1 | As 3 rotas permanecem `request_observed=false` com negativa registrada; o servidor NÃO inventa o pedido |
| 22 | O botão UPGRADE do BARRACKS emite `game/gear/upgrade` | Rig 2026-08-23, 2 reproduções: tap medido no botão (app vivo — navegação e seleção de carta respondem) dispara **re-init completo da sessão em-processo** (login-device…get-daily-quests) **sem** a chamada da rota, e o main thread do Unity trava em seguida: 0 requests, 0 linhas Unity no logcat, render thread em spam contínuo `goldfish_vulkan` QSRI (`work/final-shot/logcat-upgrade-tap.txt` — 3920/3920 linhas do pid só de vulkan) | Família upgrade/multi-upgrade permanece `request_observed=false` com negativa; causa raiz A VERIFICAR (hipótese: estado de controller nulo no handler, mesma família do NRE de `UpdateQuestState`) |
| 23 | O NRE de `DailyQuestController.UpdateQuestState` é corrida entre a resposta de `get-daily-quests` e a assinatura do evento `DailyQuestUpdated` pela UI (latência no servidor suprimiria) | Rig 2026-08-23: com atraso artificial de 1500 ms no `get-daily-quests` (`REVIVAL_QUEST_DELAY_MS` no index.js, `work/final-shot/server-fs12.log`), o NRE disparou igual no cold boot (pid 11404, 17:12:14). Refutada em sequência a hipótese irmã "estado de quests ausente no user-data": varredura dos 13886 typedefs por campos `quest`/`pending` em DTOs de Data/Response/Player não acha nada — só o próprio `GetDailyQuestsResponse` carrega quests | NRE é null **client-interno incondicional** (candidatos: campos do `DailyQuestController`/`DailyQuestRegistry`), causa raiz A VERIFICAR. Efeito medido: o callback roda DENTRO de `NetworkRequestHandler.UpdateRequestQueue` e **mata o pump de requests** — sem `reward-tracks/get-all` e sem `session/heartbeat` em qualquer boot com parse bem-sucedido; heartbeats só existiram até o request id 1004 (15:56:11), sempre na era em que o parse de quests FALHAVA |
| 24 | O travamento em `goldfish_vulkan` QSRI (0 linhas Unity, render preso) medido na entrada #22 foi CAUSADO pelo tap no botão UPGRADE | Rig 2026-08-23 (emulador `mighty-doom-lab` reiniciado do zero, `-gpu swiftshader_indirect`, conta nova `df-test-rig@example.com`, DNAT porta 80→8140 rearmada): o MESMO travamento (NRE de `UpdateQuestState` seguido de `goldfish_vulkan` QSRI em loop, 0 linhas Unity) ocorreu no cold boot **sem nenhuma interação** — nenhum tap em BARRACKS, nenhum botão tocado, cliente ainda na splash `LOADING 100%` (`work/final-shot/df/logcat-newaccount.txt`). O mesmo teste também reproduziu em outro boot totalmente limpo antes de qualquer navegação (`work/final-shot/df/df-fuse-wait.png` + logcat correspondente). Discriminador adicional: matei o processo `qemu-system-x86_64` e subi o AVD do zero com `-gpu auto` (voltando ao backend padrão, sem swiftshader) — o MESMO travamento ocorreu de novo, ainda mais rápido (antes de qualquer tap de navegação chegar a completar) (`work/final-shot/df/logcat-defaultgpu.txt`, 2953 linhas `goldfish_vulkan`) | **Refutado apenas o específico**: a escolha de backend de GPU (`swiftshader_indirect` vs `auto`) NÃO é o que decide o travamento — reproduziu idêntico nos dois. Isso é tudo que está medido. A CAUSA RAIZ do travamento continua **A VERIFICAR** — candidatas não confirmadas, sem ordem de probabilidade: (a) o pump de requests morto (entrada #23); (b) algum estado do processo do emulador/host que sobreviveu ao crash anterior desta sessão e não foi limpo por matar o `qemu-system-x86_64` sozinho. Nenhuma das duas foi isolada como causa; não afirme "driver gráfico do host corrompido" como fato — é hipótese (b), não medição. As negativas de UPGRADE/MULTI-UPGRADE (entrada #22) continuam válidas quanto ao **resultado observado** (0 wire), mas a atribuição causal ao tap específico fica sem sustentação. **DISMANTLE e FUSE nunca tiveram uma execução conclusiva neste rig** — a tentativa ficou inconclusiva pelo mesmo travamento antes de qualquer tap chegar ao botão; não classifique nenhum dos dois como confirmado (positivo ou negativo) até uma reprodução limpa com ação realmente observada. Discriminar as duas hipóteses da causa raiz exige ou (a) uma reprodução em outra máquina/AVD sem o histórico de crash desta sessão, ou (b) um reboot do HOST — ação de sistema fora do escopo autorizado sem pedido explícito do usuário |

| 25 | O travamento pós-NRE vem de estado residual do emulador/host (hipótese (b) da entrada #24) | Rig 2026-08-23: AVD descartável `mighty-doom-lab` derrubado e relançado com `-no-snapshot -wipe-data` (boot frio real, sem restaurar snapshot da sessão que quebrou), APK reinstalado do zero, DNAT rearmada, conta logada pela UI. O travamento **reproduziu idêntico**, com o NRE de `UpdateQuestState` como ÚLTIMA linha Unity antes do spam de `goldfish_vulkan` (`work/final-shot/df/clean-wedge.txt`, 3045 linhas de vulkan). Guest limpo, dados limpos, instalação limpa — e o mesmo desfecho | O ambiente **não** é a causa: some a hipótese (b). Junto com a refutação do backend de GPU (entrada #24), sobra a hipótese (a): o NRE client-interno mata o pump e o cliente trava logo depois. Continua sendo correlação forte e ordenação consistente, **não** prova de causalidade — a causa raiz do NRE em si segue A VERIFICAR. Corolário operacional: não atribua mais o travamento a "rig sujo"; o rig foi provado limpo |
| 26 | Existe um payload de `get-daily-quests` que o cliente parseia sem cair no NRE (mandar vazio, ou omitir a coleção, evitaria) | Rig 2026-08-23, bisseção de 3 degraus com env `REVIVAL_QUEST_BISECT` (experimento temporário, revertido no mesmo dia — `server/src/quests.js` voltou byte a byte): **arrays cheias** → NRE (`bisect4.txt`/logs anteriores); **as duas arrays vazias** → NRE (`work/final-shot/df/bisect1.txt`); **campos de coleção OMITIDOS** → NRE, com `Malformed response payload` = 0, ou seja o parse passou (`work/final-shot/df/bisect4.txt`). Somado ao já registrado na #23 (latência de 1500 ms não suprime). O NRE só NÃO ocorre quando o parse **falha** | O NRE é incondicional sobre QUALQUER parse bem-sucedido de `get-daily-quests`: não há conteúdo de wire que o evite. Logo **não existe correção pelo servidor** — só "consertaria" quebrar o parse de propósito, que é fabricar incompatibilidade (proibido; DEAD-ENDS #11 e #12) e regrediria o contrato medido em 4f8dc8c. Consequência medida para o planejamento: todo gate de Fase 3 que exige o cliente CHEGAR ao menu (`request_observed`/`client_validated` das rotas de UI) está bloqueado por defeito interno do cliente, não por contrato do servidor |

## Em teste (não refutado, não confirmado)

- **Stack TLS do cliente 1.13.1 × trust store do Android 14.** A CA de
  laboratório foi recusada mesmo estando em `/system/etc/security/cacerts`
  (visível dentro do namespace do processo) E declarada como trust-anchor no
  `network_security_config` (referência conferida no AXML). O trust store do
  Conscrypt vive no APEX, cuja propagação de mount é privada, e o `nsenter` do
  toybox deste build não aceita `-t`. Contornado no laboratório por HTTP
  (`scripts/patch_lab_http.py`, autorizado e restrito ao rig). **Não resolvido**
  para quem quiser TLS interceptado no Android 14 — se voltar aqui, traga
  medição nova.
