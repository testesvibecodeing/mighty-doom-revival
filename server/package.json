{
  "name": "mighty-doom-revival-server",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Standalone clean-room compatibility server for personal Mighty DOOM preservation",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "check": "node --check src/index.js && node --check src/chapters.js && node --check src/game-data-model.js && node --check src/config.js && node --check src/db.js && node --check src/store.js && node --check src/events.js && node --check test/smoke.mjs",
    "smoke": "node test/smoke.mjs",
    "test": "npm run check && npm run smoke"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
